export type FloorplanPreviewMode = "editable" | "preview" | "both";

export const FLOORPLAN_PREVIEW_MODE_LABELS: Record<FloorplanPreviewMode, string> = {
  editable: "Editable shell",
  preview: "Preview mesh",
  both: "Both",
};
